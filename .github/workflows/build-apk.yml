name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check project files
        run: |
          ls -la
          test -f index.html

      - name: Prepare build placeholder
        run: echo "Android wrapper build step will be added here"
